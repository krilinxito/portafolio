---
interface Props {
  texto: string;
  id?: string;
}
const { texto, id } = Astro.props;
---
<h2
  id={id}
  class="mb-6 font-display text-[16px] tracking-[0.1em] text-filete uppercase sm:mb-7"
>
  &gt; {texto}_
</h2>
